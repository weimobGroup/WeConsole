<template name="ca-buttons">
    <view class="ca-section">
        <button wx:for="{{buttons}}" class="ca-button {{caseLoading[item.id]?'disabled':''}}" data-id="{{item.id}}" disabled="{{caseLoading[item.id]}}" bind:tap="tapCaseButton">
            {{item.name}}
        </button>
    </view>
</template>